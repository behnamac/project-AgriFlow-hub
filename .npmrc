# Disable optional dependencies to prevent Rollup native module issues
optional=false
install-optional=false

# Force npm to use pure JavaScript implementations
target_platform=linux
target_arch=x64

# Ensure consistent installs
package-lock=true
fund=false

# Disable native modules
prefer-offline=false
audit=false
